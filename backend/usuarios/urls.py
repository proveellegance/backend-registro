from django.urls import path, include
from rest_framework.routers import DefaultRouter
from usuarios.models import CustomUser

# Para endpoints relacionados con usuarios si es necesario
urlpatterns = [
    # Los endpoints de usuarios se manejan principalmente por djoser
]
